# Only needed when the client is deployed separately from the server
# (e.g. as its own Vercel project). Leave unset for local dev — Vite's
# dev-server proxy (see vite.config.ts) handles routing "/api" locally.
VITE_API_BASE_URL=https://your-server-deployment.vercel.app/api
